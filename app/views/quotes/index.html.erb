<div class="two-thirds-panel">
  <p>
    Welcome to <em>Quote of the day</em>. It is <b> awesome</b>.
  </p>
  <ul>
    <% @quotes.each do |quote| %>
      <li><%= link_to quote.quote, quote_path(quote) %>
        (<%= link_to "Edit", edit_quote_path(quote) %> |
        <%= link_to "Delete", quote_path(quote), method: :delete, data: {confirm: 'Are you sure?'} %>)</li>
    <% end %>
  </ul>

  <p>
    <%= link_to "New quote", new_quote_path, class: "button__line" %>
  </p>
</div>
